version: '3'

services:
  web:
    build:
      context: .
    container_name: recipe-service
    env_file:
      - .env
    # volumes:
    #   - .:/code
    develop:
      watch:
        - action: sync
          path: .
          target: /code/
    ports:
      - 2000:2000